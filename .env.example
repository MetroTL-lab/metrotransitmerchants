# Copy this file to .env (for local dev with `npm run dev`) or set the
# same variable in your host's environment variable settings (e.g.
# Netlify: Site settings -> Environment variables) before building for
# production. Vite only exposes vars prefixed VITE_ to client code.
#
# Find this URL in your Supabase dashboard under Project Settings -> API
# -> "Project URL", with .supabase.co swapped for .functions.supabase.co.
VITE_FUNCTIONS_URL=https://dyartwkzpivjyhebfcnb.functions.supabase.co
